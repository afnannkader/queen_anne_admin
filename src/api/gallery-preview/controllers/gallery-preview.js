'use strict';

/**
 * gallery-preview controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::gallery-preview.gallery-preview');
